// classlist -> Gives total numbers of classes applied
const h1 = document.querySelector("h1");

console.log(h1.classList);

// Add class
// h1.classList.add("bg");

// Remove class
// h1.classList.remove("bg");

// toggle
h1.classList.toggle("bg");
